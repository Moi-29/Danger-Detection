# Optional: override WebSocket URL (default: ws://localhost:8000/ws in dev)
# VITE_WS_URL=ws://127.0.0.1:8000/ws

# Optional: REST API base for /api/events (default: same origin in prod, or http://localhost:8000 in dev)
# VITE_API_BASE=http://127.0.0.1:8000
